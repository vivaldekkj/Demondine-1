/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

cam = view_camera[0];
follow = obj_girafa;

w_half = camera_get_view_width(cam) * 0.5;
h_half = camera_get_view_height(cam) * 0.5;

targetx = xstart;

targety = ystart;

spd = 15;
